{
  "name": "relationship-os",
  "version": "1.0.0",
  "description": "Telegram relationship coach bot for couples — tracks cycle phases, stress levels, and delivers personalized daily content",
  "main": "src/bot/index.js",
  "scripts": {
    "start": "node src/bot/index.js",
    "dev": "nodemon src/bot/index.js",
    "scheduler": "node src/scheduler/index.js",
    "db:generate": "npx prisma generate",
    "db:push": "npx prisma db push",
    "db:migrate": "npx prisma migrate dev",
    "db:studio": "npx prisma studio"
  },
  "dependencies": {
    "grammy": "^1.21.1",
    "@prisma/client": "^5.10.0",
    "ioredis": "^5.3.2",
    "node-cron": "^3.0.3",
    "dotenv": "^16.4.1",
    "nanoid": "^3.3.7",
    "date-fns": "^3.3.1"
  },
  "devDependencies": {
    "prisma": "^5.10.0",
    "nodemon": "^3.0.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
