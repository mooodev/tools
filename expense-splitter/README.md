# Сочтёмся / Splitwise Bot

Telegram бот и веб-приложение для разделения расходов между группой людей.

**Telegram bot + Web App for splitting expenses between groups.**

---

## Возможности / Features

### Группы / Groups
- Создание групп (Дом, Поездка, Пара, Другое)
- Присоединение по коду приглашения
- Управление участниками

### Расходы / Expenses
- 5 способов деления: поровну, по суммам, по процентам, по долям, с корректировкой
- Множественные плательщики
- 20 категорий расходов
- Заметки и фото чеков
- Повторяющиеся расходы (еженедельно, раз в 2 недели, ежемесячно, ежегодно)

### Баланс и расчёты / Balance & Settlements
- Автоматический расчёт балансов
- Упрощение долгов (Simplify Debts) — минимизация числа переводов
- Расчёт наличными, банковским переводом, другими способами
- Напоминания о долгах

### Мультиязычность / i18n
- 🇷🇺 Русский
- 🇬🇧 English

### Дополнительно / Extra
- Мультивалютность (10+ валют)
- Экспорт данных в CSV
- Уведомления в Telegram
- Расходы между друзьями (без группы)
- Веб-интерфейс (Telegram Web App)
- Полнотекстовый поиск по расходам

---

## Технологии / Tech Stack

- **Runtime:** Node.js
- **Bot:** node-telegram-bot-api
- **Database:** SQLite (better-sqlite3)
- **Web:** Express.js
- **Cron:** node-cron (повторяющиеся расходы)

---

## Установка / Installation

### 1. Клонирование

```bash
cd expense-splitter
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```env
BOT_TOKEN=your_telegram_bot_token_here
PORT=3000
BASE_URL=https://your-domain.com
```

Получите токен бота у [@BotFather](https://t.me/BotFather) в Telegram.

### 4. Запуск

```bash
npm start
```

Для разработки:
```bash
npm run dev
```

---

## Команды бота / Bot Commands

| Команда | Описание |
|---------|----------|
| `/start` | Начало работы |
| `/lang` | Выбор языка |
| `/groups` | Мои группы |
| `/newgroup` | Создать группу |
| `/join <код>` | Присоединиться к группе |
| `/expense` | Добавить расход |
| `/balance` | Посмотреть баланс |
| `/settle` | Рассчитаться |
| `/history` | История расходов |
| `/friends` | Друзья |
| `/settings` | Настройки |
| `/export` | Экспорт в CSV |
| `/help` | Справка |

---

## Архитектура / Architecture

```
expense-splitter/
├── src/
│   ├── index.js              # Entry point
│   ├── bot/
│   │   ├── bot.js            # Telegram bot handlers
│   │   ├── keyboards.js      # Inline & reply keyboards
│   │   └── stateManager.js   # Conversation state
│   ├── db/
│   │   ├── database.js       # SQLite connection & init
│   │   └── schema.js         # Database schema
│   ├── i18n/
│   │   ├── index.js          # Translation helper
│   │   ├── ru.js             # Russian translations
│   │   └── en.js             # English translations
│   ├── services/
│   │   ├── userService.js    # User management
│   │   ├── groupService.js   # Group management
│   │   ├── expenseService.js # Expense CRUD
│   │   ├── balanceService.js # Balance & debt simplification
│   │   ├── splitCalculator.js # Split algorithms
│   │   ├── recurringService.js # Recurring expenses
│   │   ├── notificationService.js # Notifications
│   │   └── exportService.js  # CSV export
│   └── web/
│       ├── server.js         # Express API server
│       └── public/
│           ├── index.html    # Web App SPA
│           ├── css/style.css # Styles
│           └── js/
│               ├── app.js    # Frontend logic
│               └── i18n.js   # Frontend translations
├── package.json
├── .env.example
└── README.md
```

---

## Алгоритм упрощения долгов / Debt Simplification

Используется жадный алгоритм:

1. Для каждого участника вычисляется **чистый баланс** (разница между заплаченным и должным)
2. Участники делятся на **должников** (отрицательный баланс) и **кредиторов** (положительный)
3. Сортировка по убыванию суммы
4. Жадное сопоставление: наибольший должник платит наибольшему кредитору

**Пример:**
- Анна: -500₽ (должна)
- Борис: +300₽ (ему должны)
- Катя: +200₽ (ей должны)

Результат: Анна → Борис: 300₽, Анна → Катя: 200₽ (2 транзакции вместо возможных N)

---

## API

### Users
- `GET /api/user/:telegramId` — Получить пользователя
- `POST /api/user/:telegramId/language` — Установить язык
- `GET /api/user/:telegramId/groups` — Группы пользователя
- `GET /api/user/:telegramId/balance` — Общий баланс

### Groups
- `POST /api/groups` — Создать группу
- `GET /api/groups/:id` — Детали группы
- `GET /api/groups/:id/members` — Участники
- `GET /api/groups/:id/expenses` — Расходы группы
- `GET /api/groups/:id/balance` — Баланс группы
- `PUT /api/groups/:id/settings` — Настройки
- `DELETE /api/groups/:id` — Удалить группу
- `POST /api/groups/join` — Присоединиться

### Expenses
- `POST /api/expenses` — Добавить расход
- `GET /api/expenses/:id` — Детали расхода
- `DELETE /api/expenses/:id` — Удалить расход
- `POST /api/expenses/:id/comments` — Комментарий

### Settlements
- `POST /api/settlements` — Зафиксировать расчёт

### Other
- `GET /api/categories` — Категории
- `GET /api/export/:telegramId` — Экспорт CSV
- `POST /api/upload` — Загрузить чек

---

## Лицензия / License

MIT
